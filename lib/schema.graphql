type Person
@model
@auth(rules: [{ allow: public }]) {
    name: String
    birthYear: String
    gender: String
    height: Int
    mass: Float
}

type Starship
@model
@auth(rules: [{ allow: public }]) {
    name: String
    model: String
    length: Float
    crew: String
}
